"""Retrieval-Augmented Generation: chunking, embedding, vector storage, and
retrieval for dataset content, so the analysis agents in `app.ai` can ground
their reasoning in evidence from the actual uploaded files, not just the
aggregated profile statistics `app.services.dataset_profile_service` computes.
"""
