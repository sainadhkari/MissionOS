# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env
coverage/

# Ad-hoc diagnostic/scratch scripts (e.g. one-off Playwright debugging
# scripts) written directly in this tree during live debugging sessions --
# these belong in a scratch directory outside the repo, but this catches
# any that slip in before they get committed by accident.
diag_*
scratch_*

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
